/* Legacy smart-features modal removed.
   The app now uses web/aqari-feature-router.jsx for direct, full-screen feature navigation.
   This file intentionally contains no UI, modal, dialog, or click handlers. */
(()=>{'use strict';})();
